const bemNotes = `block__element → double underscore separates an element of a block.

block--modifier → double hyphen separates a modifier of a block.

Use __ for elements

Use -- for modifiers

Hyphens - are just normal word separators
`;

export default bemNotes;
